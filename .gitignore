# OS
.DS_Store
Thumbs.db

# Editor swap/backup files
*.swp
*.swo
*~

# Neovim session files
Session.vim

# Secrets — never commit
.env
.env.*
*.pem
*.key

# lazy-lock.json is intentionally tracked (pinned versions = security)
# Do NOT add lazy-lock.json here
